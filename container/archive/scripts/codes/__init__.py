# Minimal stub so metiswise.main.drld can import without the full
# METIS_Pipeline 'codes' package.  The real package parses the Data
# Reduction Library Design (DRLD) document to enumerate every data
# product.  This stub provides an empty DRLD, which is sufficient for
# database table creation — the static Raw/Pro class hierarchy already
# defines all persistent columns.
